<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Signature-level permission for the GDrive image ContentProvider.
         Only apps signed with the same key (i.e. this app) can read from it. -->
    <permission
        android:name="com.cloudamp.music.permission.READ_GDRIVE_IMAGES"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="CloudAmp"
        android:theme="@style/Theme.CloudAmp"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Now Playing Activity -->
        <activity
            android:name=".NowPlayingActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Settings Activity (handles OAuth redirect via custom URI scheme) -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.cloudamp.music"
                    android:host="oauth2callback" />
            </intent-filter>
        </activity>

        <!-- GDrive Library Activity -->
        <activity
            android:name=".GDriveLibraryActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- GDrive Structured Library Activity -->
        <activity
            android:name=".GDriveStructuredLibraryActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- GDrive Home Activity -->
        <activity
            android:name=".GDriveHomeActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Saved Queues Activity -->
        <activity
            android:name=".SavedQueuesActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Cached Albums Activity -->
        <activity
            android:name=".CachedAlbumsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Visualizer Activity -->
        <activity
            android:name=".VisualizerActivity"
            android:exported="false"
            android:theme="@style/Theme.CloudAmp.Fullscreen"
            android:parentActivityName=".NowPlayingActivity" />

        <!-- Media Browser Service for Android Auto -->
        <service
            android:name=".playback.CloudAmpService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- GDrive Image Provider for Android Auto.
             Protected by signature-level permission so only this app can access it. -->
        <provider
            android:name=".playback.GDriveImageProvider"
            android:authorities="com.cloudamp.music.gdriveimages"
            android:exported="true"
            android:readPermission="com.cloudamp.music.permission.READ_GDRIVE_IMAGES"
            android:grantUriPermissions="true" />

        <!-- Media Button Receiver -->
        <receiver android:name="androidx.media.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
    </application>

</manifest>
